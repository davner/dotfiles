# macOS + nix build noise
.DS_Store
result
result-*

# no-mistakes local validation state - never commit in this public repo
.no-mistakes/

# ticket-loop state (specs, reports, live worktrees) - local only, never committed
.tickets/